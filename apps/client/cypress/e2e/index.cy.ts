describe('Index Page', () => {
  describe('Index View', () => {
    beforeEach(() => {
      cy.visit('/');
    });

    it('should display the index page', () => {
      //   cy.get('[data-testid="index-page"]').should('exist');
    });
  });
});
